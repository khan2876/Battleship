
/**
 * This class represents a Battleship board.  It consists of an array of Cell objects for each of the cells.  
 * All calls to Cell objects should happen through here.
 */
public class Board {
    private int hitCount = 0;
    private int shotsTaken = 0;

    public Board(/* Level: Easy or Hard */) {
        // setup the initial game board
    }

    /*
     * Possible methods to make: 
     * boolean isRevealed( location, ) - to check if a cell has been fired at already
     * boolean gameOver()
     * int numShotsMade() - count of how many shots have already been fired
     * printBoard()
     * 
     */

}
